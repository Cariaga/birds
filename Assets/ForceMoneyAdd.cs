using UnityEngine;
using System.Collections;

public class ForceMoneyAdd : MonoBehaviour {

	// Use this for initialization
	void Start () {
        PlayerPrefs.SetInt("Money",9999);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
